- touching the {{ excluded_campaign }} campaign, retiring the legacy migration cron, or
  removing the migration-campaign exclusion;
- installing / cutting over crontab lines;
- the Friday `service_number` cutover.