    - `cs drive ls` (no arg) lists the Shared Drives the SA can see — `{{ drive_scope }}`
      for {{ company_name }}, `UNIROMA`, … A different company's drive (e.g. CAFE124)
      is NOT shared here and must NOT be searched from this project.