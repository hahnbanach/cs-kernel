# {{ company_name }} — required secrets (values in ~/.{{ company_slug }}-cs/.env)
# Copy to ~/.{{ company_slug }}-cs/.env and fill in.
EMAIL_PASSWORD=
FIREBASE_WEB_API_KEY=
CS_ACCOUNTS={{ accounts_default }}:{{ engine_owner_uid }}{% for name, uid in accounts.items() if name != accounts_default %},{{ name }}:{{ uid }}{% endfor %}
{% if sms_enabled %}SMS_BUSINESS_ID={{ '' }}{% endif %}
{% if crm_adapter == 'shopify' and crm_shopify %}
{{ crm_shopify.env_prefix }}_STORE_DOMAIN=
{{ crm_shopify.env_prefix }}_CLIENT_ID=
{{ crm_shopify.env_prefix }}_SECRET=
{% endif %}